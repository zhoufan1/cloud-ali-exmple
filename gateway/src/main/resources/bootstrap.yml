spring:
  application:
    name: gateway
  cloud:
    nacos:
      discovery:
        server-addr: knife.cn:8848
        metadata:
          preserved:
            heart:
              beat:
                interval: 10000
      config:
        server-addr: knife.cn:8848
        group: ${spring.application.name}
  profiles:
    active: dev
server:
  port: 8161